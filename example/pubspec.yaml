name: example
description: Flutter Bidirectional Listview Example
version: 1.0.1

dependencies:
  bidirectional_listview:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
