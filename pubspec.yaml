name: bidirectional_listview
description: ListView with items that can be scrolled in both directions with a fixed item count and scroll boundaries.
version: 1.0.2+2
homepage: https://github.com/Rodiii/flutter_bidirectional_listview
maintainer: Christoph Rothermel (@Rodiii)

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
